{
  "name": "revenue-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Truthful, quest-driven revenue engine: real agents, real ledger, no simulated money.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node src/cli.js serve",
    "status": "node src/cli.js status",
    "preview": "node scripts/build-preview.mjs",
    "export-art": "node scripts/export-art.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest"
  }
}
